# Walking-skeleton fixture context

The requested result is a `status()` value of `"ready"` and a report file at
`docs/report.txt` containing exactly `walking skeleton complete` followed by a
newline.

This file supplies task context only. It does not grant filesystem, network,
command, credential, or external-effect authority.
